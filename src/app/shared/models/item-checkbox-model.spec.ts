import { ItemCheckboxModel } from './item-checkbox-model';

describe('ItemCheckboxModel', () => {
  it('should create an instance', () => {
    expect(new ItemCheckboxModel()).toBeTruthy();
  });
});
