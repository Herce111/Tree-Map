import { ItemBreadcrumbsModel } from './item-breadcrumbs-model';

describe('ItemBreadcrumbsModel', () => {
  it('should create an instance', () => {
    expect(new ItemBreadcrumbsModel()).toBeTruthy();
  });
});
