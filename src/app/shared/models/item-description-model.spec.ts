import { ItemDescriptionModel } from './item-description-model';

describe('ItemDescriptionModel', () => {
  it('should create an instance', () => {
    expect(new ItemDescriptionModel()).toBeTruthy();
  });
});
